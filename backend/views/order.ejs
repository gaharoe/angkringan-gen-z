<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="maximum-scale=1, user-scalable=0,width=device-width, initial-scale=1.0">
    <link href="/public/libs/font-poppins.css" rel="stylesheet"/>
    <script src="/public/libs/tailwind.js"></script>
    <style>body{font-family:'Poppins';}</style>
    <title>Order</title>
</head>
<body class="flex flex-col items-center">
    </div>
    <div class="flex flex-col items-center shadow-lg shadow-[rgb(0,0,0,0.15)] border border-dashed border-gray-400 rounded-lg w-[300px] mt-1">
        <h1 class="my-5 text-orange-400">Pesanan anda</h1>
        <div class="w-[90%]">
            <%order.forEach(data => {%>
                <div class=" flex justify-between text-gray-700 border-t py-2 border-gray-400">
                    <p><%=data.name%> - x<%=data.qty%></p>
                    <p class="text-sm">Rp. <%=data.price%></p>
                </div>
            <%})%>
                
            <div class=" flex justify-between text-gray-700 border-t border-dashed py-2 mt-10 border-gray-400">
                <p class="text-md">Total:</p>
                <p class="text-md">Rp. <%=total%></p>
            </div>
        </div>
    </div>

    <div class="flex flex-col items-center shadow-lg shadow-[rgb(0,0,0,0.15)] border border-dashed border-gray-400 rounded-lg w-[300px] mt-5">
        <div class=" flex justify-between text-gray-700 py-2">
            <p class="text-md text-yellow-500"><%=status%></p>
        </div>       
    </div>
    <script>
    </script>
</body>
</html>